apiVersion: gateway.kgateway.dev/v1alpha1
kind: AgentGatewayPolicy
metadata:
  name: traffic-timeout-policy
  namespace: default
spec:
  targetRefs:
  - group: gateway.networking.k8s.io
    kind: HTTPRoute
    name: test-route
  traffic:
    timeout:
      request: 30s
      idle: 5m